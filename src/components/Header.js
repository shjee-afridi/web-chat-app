import React from "react";

// Import FontAwesomeIcon component

// Importing Component
import Signout from "./Signout";

function Header() {
  return (
    <header style={{overflowX: "scroll", overflowY: "hidden"}}>
      <Signout />
    </header>
  );
}

export default Header;
